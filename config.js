const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "localhost",
    user: "root",
    password: "Baci8899$",
    database: "nvdb",
  },
  listPerPage: 750000,
};

module.exports = config;
